# E2E 自动化测试 - 快速开始指南

## 🚀 快速开始

### 1. 进入测试目录

```bash
cd e2e-automation
```

### 2. 运行完整业务流程测试（推荐）

```bash
./run-task-assignment-test.sh --complete
```

这个命令会自动执行完整的业务流程：
- ✅ 登录系统
- ✅ 创建线索
- ✅ 创建报价（包含"公司改制"产品）
- ✅ 确认报价并生成合同
- ✅ 从合同创建工单
- ✅ 验证任务项从产品步骤加载（6个任务项）
- ✅ 测试任务项分配功能
- ✅ 验证分配结果

### 3. 查看测试结果

测试完成后，查看截图：

```bash
ls -la screenshots/
```

查看 HTML 报告：

```bash
npx playwright show-report
```

---

## 📋 其他运行模式

### UI 模式（可视化界面）

```bash
./run-task-assignment-test.sh --complete --ui
```

### 显示浏览器模式

```bash
./run-task-assignment-test.sh --complete --headed
```

### 调试模式

```bash
./run-task-assignment-test.sh --complete --debug
```

---

## 📁 文件说明

| 文件/目录 | 说明 |
|----------|------|
| `README.md` | 详细文档 |
| `QUICK_START.md` | 本文件 - 快速开始指南 |
| `playwright.config.ts` | Playwright 配置文件 |
| `run-task-assignment-test.sh` | Linux/Mac 运行脚本 |
| `tests/` | 测试文件目录 |
| `screenshots/` | 测试截图目录 |
| `docs/` | 详细文档目录 |

---

## 🔧 前置条件

### 1. 服务运行

确保后端和前端服务正在运行：

```bash
# 返回项目根目录
cd ..

# 启动所有服务
./start_all.sh

# 检查服务状态
curl http://localhost:8000/api/v1/health  # 后端
curl http://localhost:5174                 # 前端
```

### 2. Playwright 安装

首次运行时，脚本会自动安装 Playwright。

如果需要手动安装：

```bash
# 返回项目根目录
cd ..

# 安装 Playwright
pnpm add -D @playwright/test -w

# 安装浏览器
npx playwright install chromium
```

---

## 📸 测试截图

测试会自动保存以下截图：

1. `01-login-success.png` - 登录成功
2. `02-lead-created.png` - 线索创建
3. `03-quote-created.png` - 报价创建
4. `04-contract-created.png` - 合同创建
5. `05-order-created.png` - 工单创建
6. `06-order-detail.png` - 工单详情
7. `07-task-items-list.png` - 任务项列表
8. `08-executor-selected.png` - 执行人选择
9. `09-assignment-success.png` - 分配成功
10. `10-assignment-verified.png` - 分配结果验证

---

## 🐛 常见问题

### 问题1：Playwright 未安装

**解决方案**：

```bash
cd ..  # 返回项目根目录
pnpm add -D @playwright/test -w
npx playwright install chromium
```

### 问题2：服务未运行

**解决方案**：

```bash
cd ..  # 返回项目根目录
./start_all.sh
```

### 问题3：测试失败

**解决方案**：

1. 查看截图目录了解失败原因
2. 查看 HTML 报告
3. 使用调试模式运行：

```bash
./run-task-assignment-test.sh --complete --debug
```

---

## 📚 更多文档

- [完整流程测试文档](docs/COMPLETE_WORKFLOW_TEST.md)
- [WSL 环境使用指南](docs/WSL_PLAYWRIGHT_GUIDE.md)
- [测试数据准备指南](docs/prepare-test-data.md)

---

## 🎯 测试账号

- **用户名**：`admin`
- **密码**：`123456`

---

## ✨ 提示

- 首次运行建议使用 `--complete` 参数，这样会自动创建所有测试数据
- 使用 `--ui` 模式可以看到测试执行过程
- 测试失败时，查看 `screenshots/` 目录中的截图可以快速定位问题
- 所有测试都是幂等的，可以重复运行

---

**祝测试顺利！** 🎉

