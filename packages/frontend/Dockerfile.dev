# ============================================
# Vue3 Frontend Dockerfile - 开发版
# 支持热重载 (Vite HMR)
# ============================================

FROM node:20-alpine

WORKDIR /app

# 安装 pnpm
RUN corepack enable && corepack prepare pnpm@latest --activate

# 复制依赖文件
COPY package.json pnpm-lock.yaml ./

# 安装依赖
RUN pnpm install

# 暴露端口
EXPOSE 5173

# 开发模式启动 (Vite HMR)
CMD ["pnpm", "dev", "--host", "0.0.0.0"]

