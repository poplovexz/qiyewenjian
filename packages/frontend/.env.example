# 前端环境变量配置

# API 基础 URL
VITE_API_BASE_URL=http://localhost:8000/api/v1

# 应用版本（用于 Sentry release）
VITE_APP_VERSION=1.0.0

# Sentry 错误监控配置
# 获取 DSN: https://sentry.io → 项目设置 → Client Keys (DSN)
VITE_SENTRY_DSN=

# 环境标识（会自动从 MODE 获取，通常不需要手动设置）
# VITE_ENV=development

