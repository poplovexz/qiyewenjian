# 快速启动指南 - 5分钟运行测试

> **注意**：本指南同时适用于 Windows 和 Linux 环境

## 🚀 超快速开始（3个窗口）

### **窗口1：启动后端**

**Windows**:
```cmd
cd packages\backend
venv\Scripts\activate
python src\main.py
```

**Linux**:
```bash
cd packages/backend
source venv/bin/activate
python src/main.py
```

保持运行 ✅

---

### **窗口2：启动前端**

**Windows**:
```cmd
cd packages\frontend
npm run dev
```

**Linux**:
```bash
cd packages/frontend
npm run dev
```

保持运行 ✅

---

### **窗口3：运行测试**

**首次运行需要安装（只需一次）**:

**Windows**:
```cmd
npm install -D @playwright/test
npx playwright install chromium
```

**Linux**:
```bash
pnpm add -D @playwright/test -w
npx playwright install chromium
```

**运行测试**:

**Windows（UI模式 - 推荐）**:
```cmd
run-task-assignment-test.bat --ui
```

**Linux（无头模式 - 服务器环境）**:
```bash
./run-task-assignment-test.sh --headed
```

或者直接运行：

**Windows**:
```cmd
npx playwright test tests/e2e/test_task_item_assignment.spec.ts --ui
```

**Linux**:
```bash
npx playwright test tests/e2e/test_task_item_assignment.spec.ts --headed
```

---

## 🎨 **UI 模式界面说明**

运行 `--ui` 模式后，会打开一个可视化界面：

```
┌─────────────────────────────────────────────────────────┐
│  Playwright Test Runner                                │
├─────────────────────────────────────────────────────────┤
│  📁 tests/e2e/test_task_item_assignment.spec.ts        │
│    ✅ 工单任务项分配功能测试                            │
│       ✅ 完整的任务项分配流程测试                       │
├─────────────────────────────────────────────────────────┤
│  [▶️ Run] [⏸️ Pause] [⏭️ Step] [🔄 Reload]              │
├─────────────────────────────────────────────────────────┤
│  浏览器窗口（自动操作）                                 │
│  ┌───────────────────────────────────────────┐         │
│  │  🌐 http://localhost:5174                 │         │
│  │  ┌─────────────────────────────────────┐  │         │
│  │  │  登录页面 → 工单列表 → 工单详情    │  │         │
│  │  │  → 分配任务项 → 验证结果           │  │         │
│  │  └─────────────────────────────────────┘  │         │
│  └───────────────────────────────────────────┘         │
└─────────────────────────────────────────────────────────┘
```

---

## 📸 **查看结果**

### **1. 实时观察**

在 UI 模式下，您可以实时看到：
- ✅ 浏览器自动打开
- ✅ 自动输入用户名密码
- ✅ 自动点击按钮
- ✅ 自动选择下拉框
- ✅ 自动验证结果

### **2. 查看截图**

```cmd
explorer screenshots
```

会看到10张截图，记录了整个测试过程。

### **3. 查看报告**

```cmd
npx playwright show-report
```

在浏览器中打开详细的测试报告。

---

## 🎯 **测试内容**

测试会自动执行以下操作：

1. ✅ 登录系统（admin / 123456）
2. ✅ 打开服务工单列表
3. ✅ 点击第一个工单的"查看"按钮
4. ✅ 查看任务项列表
5. ✅ 点击第一个任务项的"分配"按钮
6. ✅ 选择一个执行人
7. ✅ 点击"确定"按钮
8. ✅ 验证执行人已更新
9. ✅ 验证操作日志已记录
10. ✅ 测试重新分配功能

**全程自动化，无需手动操作！**

---

## 🔧 **其他运行模式**

### **显示浏览器窗口（不使用UI界面）**

```cmd
run-task-assignment-test.bat --headed
```

或：

```cmd
npx playwright test tests/e2e/test_task_item_assignment.spec.ts --headed
```

### **调试模式（逐步执行）**

```cmd
run-task-assignment-test.bat --debug
```

或：

```cmd
npx playwright test tests/e2e/test_task_item_assignment.spec.ts --debug
```

### **无头模式（后台运行）**

```cmd
run-task-assignment-test.bat
```

或：

```cmd
npx playwright test tests/e2e/test_task_item_assignment.spec.ts
```

---

## ❓ **常见问题**

### **Q1: 提示找不到 npx 命令？**

**A**: 需要先安装 Node.js

1. 访问 https://nodejs.org/
2. 下载并安装 LTS 版本
3. 重启命令提示符

### **Q2: 后端服务启动失败？**

**A**: 检查 Python 虚拟环境

```cmd
cd packages\backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python src\main.py
```

### **Q3: 前端服务启动失败？**

**A**: 检查 npm 依赖

```cmd
cd packages\frontend
npm install
npm run dev
```

### **Q4: 测试找不到工单？**

**A**: 确保数据库中有工单数据

可以先手动创建一个工单，或者运行数据初始化脚本。

### **Q5: 测试超时？**

**A**: 增加超时时间

编辑 `playwright.config.ts`，将 `timeout` 改为 `120000`（120秒）。

---

## 📝 **完整示例**

假设您的项目在 `C:\projects\jizhang`：

### **第一次运行（完整步骤）**

**1. 打开命令提示符1**：
```cmd
cd C:\projects\jizhang\packages\backend
venv\Scripts\activate
python src\main.py
```

**2. 打开命令提示符2**：
```cmd
cd C:\projects\jizhang\packages\frontend
npm run dev
```

**3. 打开命令提示符3**：
```cmd
cd C:\projects\jizhang
npm install -D @playwright/test
npx playwright install chromium
run-task-assignment-test.bat --ui
```

### **后续运行（简化步骤）**

只需要3个命令：

**窗口1**：
```cmd
cd C:\projects\jizhang\packages\backend && venv\Scripts\activate && python src\main.py
```

**窗口2**：
```cmd
cd C:\projects\jizhang\packages\frontend && npm run dev
```

**窗口3**：
```cmd
cd C:\projects\jizhang && run-task-assignment-test.bat --ui
```

---

## 🎬 **视频演示（文字版）**

```
[00:00] 打开3个命令提示符窗口
[00:10] 窗口1：启动后端服务
[00:20] 窗口2：启动前端服务
[00:30] 窗口3：运行测试（UI模式）
[00:40] Playwright UI 界面打开
[00:50] 点击 "Run" 按钮开始测试
[01:00] 浏览器自动打开，显示登录页面
[01:10] 自动输入用户名和密码
[01:20] 自动点击登录按钮
[01:30] 自动导航到工单列表
[01:40] 自动点击第一个工单的"查看"按钮
[01:50] 工单详情页面打开
[02:00] 自动点击第一个任务项的"分配"按钮
[02:10] 分配对话框打开
[02:20] 自动选择执行人
[02:30] 自动点击"确定"按钮
[02:40] 显示成功提示消息
[02:50] 验证执行人已更新
[03:00] 验证操作日志已记录
[03:10] 测试重新分配功能
[03:20] 测试完成！显示绿色的 ✅
[03:30] 查看 screenshots 目录，有10张截图
[03:40] 运行 npx playwright show-report 查看详细报告
[03:50] 完成！
```

---

## ✨ **总结**

**最简单的运行方式**：

1. 打开3个命令提示符窗口
2. 窗口1运行后端，窗口2运行前端
3. 窗口3运行：`run-task-assignment-test.bat --ui`
4. 观察测试自动执行
5. 查看截图和报告

**推荐使用 UI 模式（`--ui`），可以清楚地看到每一步操作！**

---

**需要帮助？**

- 详细文档：`WINDOWS_TEST_GUIDE.md`
- 功能文档：`docs/task-item-assignment-feature.md`
- 测试脚本：`tests/e2e/test_task_item_assignment.spec.ts`

